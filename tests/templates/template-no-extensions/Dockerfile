FROM alpine:latest

# Test Dockerfile without extension
# This file tests that extensionless files are processed correctly

LABEL maintainer="test@example.com"
LABEL purpose="integration testing"

RUN echo "Testing extensionless file processing"

CMD ["/bin/sh"]
