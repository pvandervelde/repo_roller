[template]
name = "template-deep-nesting"
description = "Template with deeply nested directory structure (12 levels) for path handling tests"
author = "RepoRoller Test Suite"
tags = ["test", "edge-case", "deep-nesting"]
