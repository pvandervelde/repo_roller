// Intentionally broken Rust code for testing error handling
use std::collections::HashMap;
use nonexistent_crate::SomeType; // This import will fail

fn main() {
    println!("This code has intentional syntax errors");

    // Missing semicolon
    let x = 5

    // Undefined variable
    println!("{}", undefined_variable);

    // Invalid syntax
    let map = HashMap::new(
    map.insert("key", "value");

    // Unclosed function
    fn broken_function() {
        let y = 10;
        // Missing closing brace
}
